### Feature

Implement the requested behavior through design, code, and relevant verification.

1. Establish the affected behavior, ownership, and acceptance criteria from the code and task context.
2. Resolve design choices that matter. Use **architect** or a prototype when competing approaches need examination; use established patterns directly when the shape is clear.
3. Implement locally or delegate independent work with explicit ownership. Reuse existing context and review delegated artifacts.
4. Verify the changed behavior with appropriate project checks. Exercise UI or integration paths when the result depends on them.
5. Continue through fixes to a reviewable result. Use **Opening a PR** only when PR creation is authorized.

For larger work, sequence dependencies and isolate shared writes. Keep the plan proportional to the task; no fixed throughput checkpoint or recursive delegation is required.

**Reply:** what changed, consequential choices, verification, and unresolved decisions.
